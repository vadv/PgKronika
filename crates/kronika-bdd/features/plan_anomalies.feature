@web @anomalies @plan_evidence @serial
Feature: Stored query-plan evidence reaches the real anomalies API
  Optimizer choices are not forced in this scenario. Deterministic producer
  rows prove the analyzer through PGM storage and the packaged web process;
  the fork-specific collector features separately compare live PostgreSQL
  15-18 extension rows with their sealed sections.

  Scenario: a new plan mixture and higher same-plan buffer work stay distinct
    Given a deterministic OSSC plan-evidence segment
    Then a real web process reports the plan-mixture and per-call buffer signals
