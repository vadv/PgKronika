//! Steps for `features/replication_instance.feature` (type `1_015_001`).
//!
//! The feature uses only the shared vocabulary: the singleton expectation
//! table, exact oracles, and the captured-floor window pair for
//! `current_wal_lsn` (see [`super::common`]).
