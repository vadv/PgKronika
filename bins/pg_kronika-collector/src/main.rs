//! Collects and writes data on the database host. The only privileged process.
//!
//! Not implemented yet: the workspace is in the skeleton stage,
//! see `docs/plan.md`.

fn main() {
    eprintln!("pg_kronika-collector: not implemented yet, see docs/plan.md");
    std::process::exit(2);
}
