# pg_kronika-web

[English version](README.md)

`pg_kronika-web` отдаёт данные из локального корня PgKronika через встроенный
интерфейс, JSON API и точку Prometheus. Он читает готовые сегменты из дерева
`YYYY/MM/DD` и целостные кадры корневого `active.parts` через
`LocalDirSnapshot`, держит индекс событий по времени, раз в секунду обновляет
опубликованный снимок каталога и не подключается к PostgreSQL. Один
долгоживущий компонент сворачивает изменения журнала, переносит
факты из точно совпавших готовых сегментов и атомарно публикует неизменяемые
представления дескрипторов и текущих данных. Тела секций из готовых сегментов
загружаются только для допущенных запросов.

## Настройки

| Переменная | По умолчанию | Назначение |
| --- | ---: | --- |
| `KRONIKA_WEB_DIR` | обязательна | Корень данных PgKronika с `active.parts`, блокировками владельцев, файлами `YYYY/MM/DD/N.pgm` и необязательными соседними `N.ovf`. |
| `KRONIKA_WEB_ADDR` | обязательна | Адрес прослушивания в формате `host:port`. |
| `KRONIKA_WEB_BASIC_AUTH` | не задан | `user:password`; без него UI и `/v1/*` открыты. |
| `KRONIKA_WEB_STALE_AFTER_S` | `10` | `/readyz` возвращает `503`, если с последнего успешного обновления прошло больше этого времени. |
| `KRONIKA_WEB_LOG` | `info` | Уровень фильтра для `tracing-subscriber`. |
| `KRONIKA_WEB_OVERVIEW_FALLBACK_SEGMENT_HOURS` | `24` | Суммарная длительность сегментов в резервном кэше после восстановимых ошибок публикации. |
| `KRONIKA_WEB_OVERVIEW_FALLBACK_BYTES` | `67108864` | Лимит канонических байтов в резервном кэше процесса. |
| `KRONIKA_WEB_OVERVIEW_GC_MAX_ENTRIES` | `100000` | Максимальное число записей при полном просмотре каталога; достижение предела запрещает удаление. |
| `KRONIKA_WEB_OVERVIEW_GC_GRACE_GENERATIONS` | `2` | Число разных авторитетных поколений GC до удаления неактуального `.ovf`. |
| `KRONIKA_WEB_OVERVIEW_GC_WALL_GRACE_S` | `120` | Минимальное время после первого авторитетного обнаружения неактуального `.ovf`, секунды. |
| `KRONIKA_WEB_OVERVIEW_GC_ARTIFACT_GRACE_S` | `600` | Минимальный возраст распознанного временного файла публикации перед удалением, секунды. |
| `KRONIKA_WEB_OVERVIEW_CACHE_MAX_LOGICAL_BYTES` | не задан | Необязательный предел суммы логических `st_size` для распознанных сайдкар-файлов. |
| `KRONIKA_WEB_OVERVIEW_CACHE_MAX_FILES` | не задан | Необязательный предел числа распознанных сайдкар-файлов. |
| `KRONIKA_WEB_OVERVIEW_RESPONSE_CACHE_BYTES` | `8388608` | Лимит логических байтов для сериализованных ответов `/overview` и `/health`. |
| `KRONIKA_WEB_OVERVIEW_RESPONSE_CACHE_ENTRIES` | `4096` | Лимит числа сериализованных ответов `/overview` и `/health`. |
| `KRONIKA_WEB_OVERVIEW_DECODED_CACHE_BYTES` | `67108864` | Лимит памяти для декодированных фактов готовых сегментов. |
| `KRONIKA_WEB_OVERVIEW_DECODED_CACHE_ENTRIES` | `4096` | Лимит числа наборов декодированных фактов. |
| `KRONIKA_WEB_OVERVIEW_SOURCE_SCRUB_INTERVAL_S` | `60` | Интервал между потоковыми проверками CRC, секунды; за раз проверяется одна секция готового сегмента. |
| `KRONIKA_WEB_OVERVIEW_CURSOR_MAX_VIEWS` | `64` | Максимальное число представлений событий, закреплённых для продолжения по курсору. |
| `KRONIKA_WEB_OVERVIEW_CURSOR_MAX_BYTES` | `536870912` | Лимит логических байтов для закреплённых представлений событий. |
| `KRONIKA_WEB_OVERVIEW_CURSOR_TTL_S` | `300` | Время жизни курсора и закреплённого представления, секунды. |
| `KRONIKA_WEB_OVERVIEW_MAX_SELECTED_SEGMENTS` | `1024` | Лимит готовых сегментов в одном запросе временной шкалы; допустимый диапазон `1..=4096`. |
| `KRONIKA_WEB_OVERVIEW_COLD_MAX_WORKERS` | `4` | Максимальное число одновременно выполняемых сборок фактов. |
| `KRONIKA_WEB_OVERVIEW_COLD_MAX_QUEUE` | `64` | Максимальное число сборок фактов в очереди. |
| `KRONIKA_WEB_OVERVIEW_COLD_PER_REQUEST_PARALLELISM` | `4` | Максимальное число сборок, одновременно начатых одним запросом. |
| `KRONIKA_WEB_OVERVIEW_COLD_WAIT_TIMEOUT_MS` | `5000` | Максимальное время ожидания в очереди FIFO, миллисекунды. |
| `KRONIKA_WEB_OVERVIEW_COLD_RETRY_AFTER_S` | `1` | Значение `Retry-After` при перегрузке сборки фактов. |
| `KRONIKA_WEB_OVERVIEW_COLD_PGM_BYTES` | `1073741824` | Общий лимит байтов исходных PGM для выполняемых сборок. |
| `KRONIKA_WEB_OVERVIEW_COLD_DECODED_BYTES` | `1073741824` | Общий лимит декодированных байтов для выполняемых сборок. |
| `KRONIKA_WEB_OVERVIEW_COLD_CPU_ROWS` | `2097152` | Общая оценка числа строк, обрабатываемых выполняемыми сборками. |
| `KRONIKA_WEB_OVERVIEW_COLD_FILE_DESCRIPTORS` | `16` | Общий резерв файловых дескрипторов для выполняемых сборок. |
| `KRONIKA_WEB_OVERVIEW_COLD_READ_BYTES` | `1073741824` | Общий лимит чтения PGM и сайдкар `.ovf`. |
| `KRONIKA_WEB_OVERVIEW_COLD_WRITE_BYTES` | `1073741824` | Общий лимит записи сайдкар `.ovf`. |
| `KRONIKA_WEB_OVERVIEW_COLD_PUBLICATIONS` | `4` | Максимальное число одновременно выполняемых публикаций `.ovf`. |

```sh
KRONIKA_WEB_DIR=/var/lib/pg_kronika \
KRONIKA_WEB_ADDR=127.0.0.1:8688 \
KRONIKA_WEB_BASIC_AUTH='operator:change-me' \
pg_kronika-web
```

TLS не встроен: слушайте только локальный интерфейс или используйте обратный
прокси с TLS. Basic Auth защищает встроенный интерфейс и `/v1/*`; `/healthz`,
`/readyz` и `/metrics` всегда открыты. Учётные данные скрываются в ошибках
конфигурации и диагностическом выводе, но сама Basic Auth соединение не
шифрует.

Значения по умолчанию и ограничения ресурсов временной шкалы:

| Ресурс | По умолчанию | Ограничение |
| --- | ---: | ---: |
| Резервный кэш после восстановимой ошибки публикации | 24 сегмент-часа, 64 МиБ | 744 часа, 256 МиБ |
| Просмотр каталога при GC | 100 000 записей | 1 000 000 записей |
| Ожидание перед удалением неактуального `.ovf` | 2 разных авторитетных поколения и 120 с | Оба значения ненулевые; поколений не меньше 2 |
| Ожидание для временных файлов публикации | 600 с | Ненулевое значение |
| Квота сайдкар-файлов | Без лимита байтов и файлов | Необязательные ненулевые пределы логических байтов и числа файлов |
| Сериализованные ответы `/overview` и `/health` | 4 096 записей, 8 МиБ | Оба лимита ненулевые и помещаются в `usize` |
| Декодированные факты в памяти | 4 096 наборов, 64 МиБ | Попадание не требует допуска новой сборки; оба лимита ненулевые и помещаются в `usize` |
| Потоковая проверка источника | Одна секция каждые 60 с | Интервал ненулевой; ошибка CRC исключает источник из набора пригодных дескрипторов |
| Представления событий, закреплённые курсорами | 64 представления, 512 МиБ, TTL 300 с | Все лимиты ненулевые; число и байты помещаются в `usize` |
| Готовые сегменты в одном запросе | 1 024 | От 1 до абсолютного предела v1: 4 096 |
| Планировщик сборки фактов | 4 исполнителя, FIFO-очередь 64, не более 4 сборок от одного запроса, ожидание 5 с | Все пределы ненулевые; отказ очереди, превышение веса и тайм-аут возвращают настроенный `Retry-After` |
| Взвешенные ресурсы сборки | По 1 ГиБ на исходные, декодированные, прочитанные и записанные байты; 2 097 152 строк; 16 файловых дескрипторов; 4 публикации | Общие для процесса пределы; байты и строки округляются вверх по фиксированным квантам |
| Период запроса временной шкалы | — | 31 сутки |
| Материализованный запрос временной шкалы | — | 64 МиБ для копий наблюдений; 1 048 576 наблюдений и входных счётчиков, 262 144 обрезанных интервала покрытия, 65 536 совместных ключей, 1 024 ключа сигналов |
| Проекция аномалий | По 50 результатов каждого класса подтверждений | 10 000 положений окна; 50 000 000 тарифицируемых пар «юнит временного ряда/положение»; независимо по 10 000 числовых эпизодов и сигналов планов; на страницу секции не более 262 144 строк, 10 000 000 ячеек и 64 МиБ собственных данных переменной длины |
| Страница `/events` | 100 элементов | 1 000 элементов |
| Превью примечательных событий | 100 элементов | Фиксируется правилами `NotablePolicy::v1` |
| Линия здоровья | — | 2 000 точек |

## Режим ожидания

Live fold держит декодированный журнал между записями сегментов с общим
пределом 64 МиБ. Через 60 секунд без запросов (пробы и `/metrics` не
считаются) процесс уходит в ожидание: публикует представление с пустым
live, очищает кэши декодированных фактов и сериализованных ответов и
возвращает свободные страницы аллокатора ОС. Сканирование хранилища
продолжается каждую секунду, поэтому готовность не стареет; следующий
запрос заново сворачивает текущий журнал в течение одного тика.
`kronika_web_standby` показывает состояние.

Числовые параметры `KRONIKA_WEB_OVERVIEW_*` задаются беззнаковыми десятичными
целыми. Обязательные бюджеты, интервалы, размеры очереди и взвешенные ёмкости
должны быть ненулевыми; оба предела сайдкар-файлов можно не задавать. Значения
байтов, записей, очереди и представлений, которые становятся размерами структур
процесса, должны помещаться в `usize`. Резервный кэш дополнительно отклоняет
значения больше 744 сегмент-часов или 268435456 байт. Лимит выбранных сегментов
должен быть в диапазоне `1..=4096`. Неверное значение завершает процесс до
открытия порта.

## Сайдкар-файлы фактов

`KRONIKA_WEB_DIR` — один корень данных, целиком принадлежащий PgKronika.
Отдельного корня для производных данных и дополнительного идентификатора
хранилища нет. Готовый сегмент и его факты имеют одинаковое основание имени и
лежат в одном каталоге дня UTC:

```text
/data/active.parts
/data/.pgkronika-overview.owner.lock
/data/YYYY/MM/DD/N.pgm
/data/YYYY/MM/DD/N.ovf
```

`N` — это `SegmentId`, метка Unix в микросекундах для первого окна сбора,
которое успешно добавлено в исходный сегмент. День UTC вычисляется из этого
идентификатора, а не из диапазона запроса или времени файла. Сегмент,
пересекающий полночь, остаётся в каталоге дня начала.

`FactStore` получает проверенный `SegmentAddress` и берёт из него канонические
имена `N.pgm` и `N.ovf` в одном каталоге UTC-дня. Строка из запроса не может
выбрать другой путь. Первый `FactStore`, захвативший корневой файл
`.pgkronika-overview.owner.lock`, удерживает исключительное право изменять
сайдкар-файлы во всём дереве данных. Другие процессы могут читать полностью
проверенные `.ovf`. При конфликте публикации или GC новые факты остаются в
ограниченном резервном кэше процесса.

Каждый перечень строится строгим ограниченным обходом. Файл PGM/OVF
непосредственно в корне, символьная ссылка, неизвестный объект, неверная дата
или сегмент в чужом дне UTC прерывает обход вместо возврата частичного
представления. Это единственная поддерживаемая структура; предыдущего
публичного формата каталога у проекта не было.

Веб-процесс запускает GC после каждых 60 успешных публикаций. Число поколений
увеличивается только после нового полного и авторитетного просмотра каталога, а
не при обычном обновлении. По умолчанию удаление требует двух таких поколений и
120 секунд с первого подтверждения, что исходного сегмента больше нет.
Недоступный сегмент, ошибка просмотра или достижение лимита запрещают удаление
и не увеличивают число поколений. GC просматривает дерево напрямую, допускает
`.ovf` только после проверки заголовка PGKOVF по соответствующему PGM, не
переходит по символьным ссылкам и не удаляет PGM, `active.parts` или блокировку
владельца.

Необязательные пределы учитывают логический размер и число распознанных `.ovf`
и временных файлов публикации. Это не свободное место и не физическая квота
файловой системы. Если новый `.ovf` не помещается в полностью учтённый объём
сайдкар-файлов, ответ использует резервный кэш. При `ENOSPC` или исчерпании
квоты выполняется не более одного авторитетного прохода GC и одной повторной
публикации.

Задержка повторной записи не блокирует чтение корректных `.ovf`. Даже если
новых фактов нет, обновление запускает одну созревшую проверку восстановления.
После ошибок доступа и «только чтение» первая проверка выполняется через пять
минут. При нехватке места и временных ошибках I/O действует экспоненциальная
задержка с джиттером (для каждого экземпляра отдельно) и пределом пять минут.
Ошибки пути, состояния сайдкар-файла, соответствия источнику и
неклассифицированные ошибки I/O сообщаются без общей задержки.

## Конечные точки

Для знакомства с хранилищем сначала вызовите `/v1/ui/catalog`,
`/v1/views/summary`, `/v1/sections` и `/v1/segments`. Эти методы показывают,
какие данные доступны, до чтения строк и запуска анализа.

| Конечная точка | Параметры | Что возвращает |
| --- | --- | --- |
| `GET /healthz` | нет | Подтверждает, что HTTP-процесс работает. |
| `GET /readyz` | нет | Показывает системе мониторинга, успевает ли сервер обновлять снимок каталога, и сообщает возраст последнего успешного обновления. |
| `GET /metrics` | нет | Отдаёт метрики Prometheus об ошибках чтения, возрасте данных, HTTP-запросах, RSS и открытых файловых дескрипторах. |
| `GET /v1/version` | нет | Сообщает версию JSON API и версию формата PGM, которые обслуживает эта сборка. |
| `GET /v1/ui/context` | `at` | Возвращает один точный снимок контекста экземпляра, хоста, баз данных и репликации. |
| `GET /v1/ui/catalog` | необязательный заголовок `If-None-Match` | Возвращает девять стабильных UI-представлений: inputs, joins, формулы метрик, колонки, presets, capabilities, availability и машинные причины недоступности. Читает только метаданные PGM-каталогов, возвращает сильный ETag и отвечает `304`, если validator совпал. |
| `GET /v1/views/summary` | `at` | Возвращает для всех девяти UI-представлений последние точные population, status, `notable_level` и `notable_count` не позже курсора. Для готовых данных читает только общий блок `UiSummary` из OVF; текущий активный хвост добавляется из памяти. |
| `GET /v1/timeline/spine` | `from`, `to`; необязательный `buckets` | Возвращает выровненные ряды нагрузки на CPU и I/O PSI из индексных блоков OVF без открытия тел PGM. Полуоткрытый диапазон ограничен 24 часами, ответ — 256 КиБ. |
| `GET /v1/timeline/heatmap` | `view`, `metric`, `from`, `to`; необязательные `buckets`, `top` | Объединяет локальные top-K выбранного представления в ограниченную heatmap со score bounds и доказательством точности ранжирования. Полуоткрытый диапазон ограничен 24 часами, `buckets` — `1..=256`, `top` — `1..=64`, сериализованный ответ — 512 КиБ. Для готовых сегментов читается только `EntitySeries(view)`, тело PGM не открывается. |
| `GET /v1/frame/{view}` | обязательный `at`; необязательные `span`, `preset` или `columns`, `database`, `q`, `sort`, `order`, `limit`, `cursor` | Возвращает одну точную страницу любого из девяти UI-представлений после серверной фильтрации. Ячейки — голые значения, `null` при отсутствии данных; числовые classifications рассчитывает сервер. |
| `GET /v1/entity/{view}/{entity}` | точка: `at`, необязательный `include=related`; история: `from`, `to`, `columns`, необязательные `limit`, `cursor` | Разрешает непрозрачный токен сущности из frame. Точечный режим возвращает точные поля и доказанные связи, исторический — ограниченную страницу не более чем из 32 выбранных сегментов. |
| `GET /v1/data/quality` | `from`, `to` | Возвращает покрытие сохранённых данных, разрывы, свежесть, состояние producer, capabilities, целостность и факты качества без чтения тел секций. Диапазон ограничен 24 часами, ответ — 512 КиБ. |
| `GET /v1/storage` | нет | Возвращает ограниченный учёт PGM, OVF, журнала и карантина, ёмкость файловой системы, состояние retention, прогноз и счётчики целостности. |
| `GET /v1/sections` | нет | Показывает доступные наборы данных, их семантику, ключ сортировки и объединённый список зарегистрированных колонок. |
| `GET /v1/segments` | `from`, `to` | Показывает сегменты, пересекающие выбранный период, и число строк в каждой секции. Читает только метаданные каталога, не тела секций. |
| `GET /v1/section/{name}` | `from`, `to`; необязательные `limit`, `cursor` | Возвращает строки выбранного набора данных в порядке времени. В `gaps` указаны отсутствующие или нечитаемые интервалы, `next_cursor` появляется, если осталась следующая страница. |
| `GET /v1/sections/batch` | `from`, `to`, список `names` через запятую; необязательный `limit` | Возвращает такие же страницы строк для нескольких наборов данных, по ключу с именем секции, за один проход по пересекающимся сегментам. |
| `GET /v1/section/{name}/diff` | `from`, `to` | Преобразует накопительные счётчики в дельты и скорости в секунду для каждого объекта. Точка содержит `delta`, `rate` и `dt_micros` либо причину `nodata`, если корректную скорость вычислить нельзя. |
| `GET /v1/sections/batch/diff` | `from`, `to`, список `names` через запятую | Возвращает такой же расчёт дельт для нескольких наборов данных, по ключу с именем секции, за один проход по сегментам. |
| `GET /v1/timeline/overview` | `from`, `to` | Возвращает сводку событий, ограниченный список примечательных событий, сводку здоровья, покрытие, свежесть, полноту, точность, смысл счётчиков и известные потери. |
| `GET /v1/timeline/events` | `from`, `to`; необязательные `limit`, `cursor`, `min_severity`, `kind` | Возвращает стабильную страницу типизированных примечательных фактов событий и непрозрачный курсор, если события ещё остались. |
| `GET /v1/timeline/health` | `from`, `to`; необязательный `step` как целое число микросекунд | Возвращает не более 2 000 точек здоровья с применёнными при вычислении правилами, покрытием и фактическим шагом. |
| `GET /v1/anomalies` | `from`, `to`; необязательные `window`, `step`, `threshold`, `eps_rel`, `limit`, `section` | Находит необычные интервалы скоростей и мгновенных значений. Для сохранённых планов отдельно сообщает об изменении смеси планов с нормализацией по вызовам и о росте буферной работы того же плана. Возвращает ранжированные `episodes` и `plan_signals`, счётчики проверки, применимость и качество анализа планов, покрытие, усечение и пропущенную работу. |
| `GET /v1/incidents` | `from`, `to`; необязательные `window`, `step`, `threshold`, `eps_rel`, `epsilon`, `max_cluster_span`, `section` | Объединяет близкие по времени аномальные эпизоды в кандидаты на инциденты. Каждый инцидент содержит стабильные focus-коды, время пика, уровень политики, числа findings и совпадений, пределы confidence и только доказанные связи сущностей; ответ также сообщает покрытие, качество данных, состояние каталога и пропущенную работу. |
| `GET /` | нет | Открывает встроенный браузерный интерфейс над тем же локальным снимком данных. |

`from` и `to` — знаковые временные отметки Unix в микросекундах. Параметры
длительности принимают `250ms`, `90s`, `15m`, `2h` или секунды без суффикса.
По умолчанию методы чтения строк возвращают 1 000 строк и ограничивают `limit`
значением 10 000. Содержимое курсора непрозрачно для клиента: передавайте его в
следующий запрос без изменений.

Сервер читает один локальный корень данных PgKronika. В этом контракте нет
операции `/v1/sources`, query-параметра `source` и курсоров с привязкой к
источнику.

Каждый элемент массива `views` в ответе `/v1/views/summary` содержит
обязательное поле `collection`, которое может быть `null`. Оно равно `null`,
если к выбранному снимку не относится маркер сбора. В остальных случаях объект
содержит четыре обязательных поля:

- `collected` — число строк снимка, сохранённых в сегменте;
- `source_total` — точное число строк источника либо `null`, если чтение не
  позволило его доказать;
- `read_state` — одно из `complete`, `source_limit`, `permission`,
  `read_failure`, `collector_limit_or_loss`;
- `visibility` — одно из `full`, `restricted`, `unknown`.

UI-каталог использует закрытый набор состояний `available`, `gated`,
`not_collected` и `unsupported_type`. В представлении `processes` нет
колонки `pss`, пока collector не пишет ограниченный `smaps_rollup`;
Activity CPU и I/O требуют одновременно activity и process inputs. Размер
сериализованного каталога жёстко ограничен 512 КиБ.

В heatmap отсутствие измерения остаётся `null`, а наблюдённый ноль — `0`.
`ranking.exact` равен `true` только тогда, когда нижняя граница каждого
возвращённого score превосходит все последующие и невидимые верхние границы.
Неполный блок отражается в quality и запрещает точное доказательство. Блоки
web-index из текущего `active.parts` объединяются тем же путём из памяти.

Frame выбирает последний точный snapshot не позже `at` и доказанного
предшественника для формул с delta. Он открывает не более одного текущего и
одного predecessor PGM; sparks читают только `UiSummary` и блоки
`EntitySeries` выбранного view из OVF. `span` по умолчанию равен одному часу и
ограничен 24 часами. `limit` по умолчанию равен 100 и ограничен 200,
декодированный `q` — 256 байтами UTF-8, непрозрачный cursor — 512 байтами,
существующая query string — 8 192 байтами, JSON — 1 МиБ. Усечение ответа
происходит только между строками. Неизвестные, повторные и неверные параметры
проекции отклоняются до I/O с PGM. Cursor связан с revision проекции, точным
snapshot и нормализованным запросом.

Активны ровно 14 per-cell bindings:
`activity.query_duration_us`, `activity.transaction_duration_us`,
`statements.ms_per_row`, `statements.mean`, `statements.time_pct`,
`statements.plan_time_pct`, `tables.dead_pct`, `tables.dead_tuples`,
`tables.seq_scan_pct`, `tables.modified_since_analyze`,
`tables.inserted_since_vacuum`, `tables.autovacuum_age_seconds`,
`tables.autoanalyze_age_seconds` и `processes.rss`. Остальные 55 значений
`MetricId` представлены в threshold manifest типизированными deferred-записями.
У колонки без binding нет записи классификации; bound-колонка с отсутствующим,
gated, reset, разорванным, неверным или неприменимым входом возвращает
`not_classified` с точной причиной. Такой вход никогда не подменяется нулём.
Числа порогов, операторы, zero semantics, boundary и evidence приходят из
`kronika-analytics`; строковые formula в catalog являются описанием и не
вычисляются.

Config-bound правила autovacuum остаются deferred, пока relation reloptions не
будут устойчиво собираться. Встроенный HTML-макет не менялся.

Диапазон временной шкалы `from`/`to` полуоткрытый и не может превышать 31
сутки. `/health` принимает в `step` целое число микросекунд и при
необходимости увеличивает его, чтобы ответ не превышал 2 000 точек. До поиска
готового ответа, регистрации общей задачи, допуска анализа и закрепления
нового курсора первый запрос страницы выбирает пересекающиеся дескрипторы
готовых сегментов. Превышение лимита возвращает `400` с
`code=query_limit_exceeded` и `params.resource=selected_segments`. Данные
активного журнала не считаются готовыми сегментами и ограничиваются отдельно.
`/events` по умолчанию возвращает 100 фактов и никогда больше 1 000.
Неверный курсор или курсор от другого запроса даёт `400`. Истёкший курсор, в
том числе после перезапуска, даёт `410` с `code=cursor_expired`; вытесненное или
иначе отсутствующее закреплённое представление — `410` с `code=view_gone`.
Нехватка ёмкости реестра даёт `503` с `code=cursor_capacity_unavailable` без
`Retry-After`.

```sh
curl -u operator:change-me \
  'http://127.0.0.1:8688/v1/segments?from=0&to=9223372036854775807'
```

Успешные ответы с данными не зависят от языка. `Accept-Language` не меняет
ответы, а `/v1` не отправляет `Content-Language` и языковой `Vary`. Строки из
PostgreSQL, ОС и пользовательского ввода остаются как есть; продуктовые
подписи и объяснения принадлежат UI.

Каждая прикладная ошибка `/v1` — объект `application/json` с полями `code` и
открытым `params`. Заголовки `WWW-Authenticate`, `Allow` и `Retry-After`
сохраняются там, где их требует HTTP. Неизвестная секция даёт `404`, неверные
параметры — `400`, а превышение ограничений входных данных и материализации —
`413`. Превышение лимита выбранных готовых сегментов считается ошибкой формы
запроса и возвращает `400`. API описан в генерируемом многофайловом
[OpenAPI](openapi/openapi.yaml) и
[проекте типизированного OpenAPI](../../docs/superpowers/specs/2026-07-29-typed-multifile-openapi-design.md).

`src/api_docs.rs` — единый реестр, подключающий документированные обработчики
к Axum и OpenAPI. Rust DTO ответов определяют wire-схемы. Exporter группирует
пути и схемы по доменам, записывает стандартные относительные `$ref` и обратной
сборкой доказывает равенство дерева документу `/openapi.json`. Обновление:

```sh
make openapi
```

CI отклоняет diff в сгенерированном дереве. Библиотечные контрактные тесты
требуют именованные JSON-схемы успешных ответов, явные статусы и доменные tags
для всех 21 операции. По требованию `make openapi-bundle` создаёт некоммитный
однофайловый bundle в `target/`.

Локальная проверка уже запущенного demo:

```sh
make demo-api-smoke
```

Команда находит сохранённый диапазон, секцию и доступную heatmap-метрику, затем
Schemathesis проверяет по одному ответу каждой операции и требует характерное
для неё доказательство данных. При необходимости задаются `DEMO_API_URL` и
`PG_KRONIKA_SMOKE_AUTH=user:password`. По умолчанию отсутствие сохранённых
данных сразу завершает проверку с ошибкой; `DEMO_API_WAIT_SECONDS` включает
ограниченное ожидание. Документированный отказ
`analytic_capacity_unavailable` повторяется не более двух раз с секундной
паузой; другие ошибки не повторяются.

Workflow `Demo API smoke` в GitHub Actions запускает ту же проверку вручную для
выбранной при запуске ревизии. Он собирает и поднимает изолированный demo с
обычной 15-минутной ротацией коллектора, ждёт сохранённые данные не более
20 минут и всегда загружает диагностику стенда. Автоматический запуск при
`push` и pull request не настроен.

## Контракты чтения и анализа

- Запрос строк читает только пересекающиеся сегменты, проверяет CRC формата PGM
  и секций до декодирования, сводит зарегистрированные версии схемы под одним
  логическим именем секции и сортирует по ключу реестра. Совпадающая запись из
  готового сегмента и `active.parts` попадает в ответ один раз.
- Сводки `/overview` и страницы `/events` используют одну типизированную
  проекцию `EventFact`: смысловой `event_id`, привязанный к lineage
  `event_instance_id`, поля времени, классы примечательности и подтверждений,
  показатели качества, типизированную нагрузку, подтверждающие данные и
  сведения о потерях. Порядок страниц точно равен
  `(sort_ts_us, event_id, event_instance_id)`.
- При обновлении временной шкалы публикуются полученные из каталогов дескрипторы
  готовых сегментов и одно ограниченное поколение текущих данных; тела секций
  при этом не декодируются. Допущенный запрос загружает только факты из
  сегментов, выбранных для его интервала. Попадания в память, в
  корректный сайдкар `.ovf` или в резервный кэш после ошибки публикации не
  требуют допуска сборки из PGM. Одновременные запросы отсутствующих фактов делят
  работу по полному `FactBuildKey`; сборка продолжается после отмены запроса и
  проходит через общий настраиваемый FIFO-планировщик. Переполнение очереди,
  превышение веса или истечение времени ожидания дают `503` с
  `code=cold_build_overloaded` и настроенным `Retry-After`. Пока первый
  допущенный запрос не построил сайдкар, читающие индекс точки обслуживают это
  окно как типизированное ожидание индекса, а не как сбой: неразрешённые
  представления summary отвечают `unavailable`, spine сообщает диапазон в
  `gaps` с `reason=index_pending`, ранжирование heatmap для этого окна
  остаётся неточным, а спарклайны frame помечаются неполными.
- Если выбранный готовый сегмент не читается во время загрузки фактов, он не
  превращается в успешный пустой сегмент и не рушит весь запрос ошибкой. Ответ
  `200` содержит его интервал в `known_gaps`, пониженную полноту и идентификатор
  набора фактов, отличный от запланированного полного набора. Такой частичный
  ответ не сохраняется под ключом полного ответа. Фоновая потоковая проверка CRC
  обнаруживает скрытое повреждение тела секции, помечает сегмент недоступным и
  не позволяет старому `.ovf` спрятать разрыв.
- Счётчики событий используют проверяемую арифметику. Суммы по примечательности
  и категориям, группы SQLSTATE `top/other/missing` и совместные группы
  `top/other` независимо сходятся с числом сохранённых ошибок. Сохранённые
  группы и физические строки наблюдений считаются отдельно. Точность сохранённых
  данных, полнота данных, смысл физического счётчика, свежесть и известные
  потери остаются независимыми полями ответа.
- Корректный сайдкар `.ovf` всегда проверяется до резервного кэша процесса. Кэш
  заполняется только после восстановимой ошибки публикации. Хранилище точных
  ответов `/overview` и `/health` ограничено числом записей и байтами. Курсоры
  событий закрепляют точное неизменяемое представление в реестре с ограничениями
  по числу, байтам и времени жизни. Курсор связывает канонический набор
  запрос, правила и последнюю позицию сортировки со случайным ключом
  процесса, полученным от ОС.
- Ответ `/diff` отличает измеренный ноль от отсутствующего результата. Точка без
  корректной скорости содержит один из кодов: `reset`, `gap`, `first_point`,
  `anomaly` или `not_collected`. В этом API `anomaly` означает, что время не
  продвинулось вперёд или типы чисел не совпали.
- Поиск аномалий сравнивает каждое текущее окно с остальными пригодными точками
  выбранного периода. Первым идёт эпизод с наибольшим `abs(peak.m)`.
  При равном балле порядок однозначно задают секция, колонка/измерение,
  идентичность и границы интервала, поэтому порядок сбора не влияет на
  усечение. Объект
  `sections` считает проверенные и непроверенные положения окна, а
  `nodata_points` даёт только общую сумму: ответ `/anomalies` не разбивает её на
  сбросы, пропуски и интервалы выключенного сбора. Положение окна, пересекающее
  разрыв временного ряда, учитывается в `not_evaluated.discontinuity`.
  Пропущенные данные не заменяются нулями.
- Для сохранённых планов есть два стабильных вида подтверждений. Для upstream
  `pg_store_plans` сигнал `pg.query.plan_distribution_shift.v1` сравнивает
  смесь планов одного `query_id` ядра PostgreSQL: приращения `calls`
  нормализуются в доли планов. Нужно не менее 20 вызовов с каждой стороны и
  расстояние полной вариации не меньше `0.20`. В форке vadv строка
  идентифицируется как `(dbid, userid, planid)`, поэтому
  `queryid_stat_statements` остаётся приблизительной атрибуцией, а смесь планов
  явно помечается неприменимой.
- `pg.plan.buffer_work_per_call_increase.v1` работает для обоих поддерживаемых
  форков. Сравнивается одна и та же сохранённая идентичность плана, накопленные
  буферные счётчики делятся на приращение `calls`; нужны не менее 20 вызовов с
  каждой стороны, прирост не меньше `1` блока на вызов и не меньше `50%`.
  Shared и local `hit/read/dirtied/written`, а также temp `read/written`
  остаются десятью разными измерениями. Счётчиков temp hit и dirtied расширение
  не предоставляет.
- Окно плана не пересекает границы сброса, версии расширения, экземпляра,
  разрыва покрытия или наблюдаемого вытеснения. Каждому снимку планов нужна
  строка reset-метаданных с точно тем же `ts`; более старая строка не считается
  подтверждением происхождения. Для вывода о смеси планов нужен полный снимок.
  Сохранённую строку плана из доказанного top-N снимка можно
  использовать для её собственного сравнения буферов, но полнота генеральной
  совокупности всё равно остаётся частичной. Отсутствующие или разбитые на
  страницы данные планов и происхождения, диапазоны пропусков считывателя,
  некорректные строки, противоречивые покрытие или метаданные, отсутствующая
  системная идентичность, неподдерживаемая версия, лимит работы и усечение
  подтверждений явно отражаются в `plan_analysis.quality`, верхних `coverage`,
  `truncation`, `complete` и `status`. Отсутствие источника считается полным
  только при доказанной полноте этих входов; иначе статус анализа плана —
  `partial`.
- Верхнее поле `coverage.plan_positions_evaluated` считает положения
  специализированных детекторов с итогом stable/changed. Оно участвует в
  выборе `no_data`, `insufficient_data` и `calm` независимо от положений
  универсального детектора.
- Оба детектора ретроспективны: опора — остальная часть того же непрерывного
  участка выбранного периода, в том числе более поздние отсчёты. Сами по себе
  они не диагностируют регрессию оптимизатора и не активируют причинный вывод
  инцидента `PG-PLAN-002`. Параметр `limit` независимо ограничивает
  ранжированные числовые `episodes` и `plan_signals`; числа отброшенных
  результатов сообщаются раздельно.
- Группировка инцидентов детальнее показывает неполные входные данные:
  `data_quality` отдельно считает `resets`, `gaps` и `not_collected`,
  `coverage_by_section` перечисляет интервалы без покрытия, а `skipped`
  объясняет работу, отброшенную из-за лимита. Период запроса ограничен 24
  часами; также ограничены юниты, секции, материализованные ячейки, точки
  рядов, байты идентификаторов, объём оценки и число эпизодов.
- Каталог инцидентов вычисляет и публикует 28 основных линз, 14 веток событий,
  42 ветки анализа, 40 уникальных активных ID, ноль неактивных ID и 24
  незакрытых строгих требования `EntityJoin`. Поле `evaluators` разделяет
  основные и событийные ветки и сообщает, выполнялась ли каждая из них в этом
  запросе. `registered_lens_ids` содержит ограниченный набор из 40 стабильных
  ID, а `evaluated_lens_ids` — только ID веток, фактически допущенных к
  оценке.
- `catalog_available` сообщает о доступности каталога.
  `diagnosis_available` имеет отдельный смысл и становится истинным, только
  если выполнена хотя бы одна ветка анализа; результат (`finding`) для этого
  не нужен. Для `no_data`, отсутствующей или противоречивой идентичности узла,
  а также запроса, в котором ни одна ветка не дошла до оценки из-за
  отсутствующего входа или отказа при допуске, значение остаётся ложным.
- Каждая активная основная линза публикует только объявленное строгое
  требование `EntityJoin`, если оно есть. Требование содержит не зависящий от
  локали машинный ID, идентичность линзы-владельца (`domain`, `name`, `value`),
  контракт, тип активации и три отдельных условия: `producer`, `provenance` и
  `coverage`. Все 24 требования остаются `unavailable`: ответ не активирует и
  не подразумевает недоказанную связь. Старые поля `applied`, `active_count`,
  `catalog_count` и `dormant` оставлены для обратной совместимости. Поле
  `applied` содержит тот же набор ID в прежнем порядке; новым клиентам следует
  использовать канонические ID и `counts`.
- Подтверждение связи блокировок между секциями требует явно сохранённого
  токена производителя для общего наблюдения и точного совпадения
  `(snapshot timestamp, PID, backend_start)`. Равные временные отметки сами по
  себе связь не доказывают. Текущие сборщики активности и блокировок выполняют
  разные запросы, поэтому `entity_join.activity_lock_waiter` сообщает условия
  `producer`, `provenance` и `coverage` как недоступные.
- Имеющиеся входы ОС могут выделять ограниченный набор кандидатов, но
  совпадение кандидатов помечается как `unproven`. Точные признаки связи
  хранилища с блочным устройством и процесса с устройством cgroup
  зарезервированы для будущего запроса, в котором выполнены опубликованные
  условия `LifetimeMapping`.
- Объяснения неполного результата используют закрытую схему
  `{ "kind": "...", "params": { ... } }`. Идентификаторы представлений,
  значения перечислений, формулы, единицы и подтверждения остаются стабильными
  машинными данными; в каталоге инцидентов нет локализованных `title` и
  `question`.
- Одновременно выполняется одна тяжёлая проекция `/anomalies`, `/incidents`
  или временной шкалы без готового ответа. Одинаковые отсутствующие
  ответы разделяют одну общую задачу, а готовые ответы не занимают слот
  исполнителя. Другой отдельный тяжёлый запрос ждёт слот до 2 секунд и лишь
  затем получает `503` с `code=analytic_capacity_unavailable` и
  `Retry-After: 1`.

Предупреждения сканирования и повреждённые диапазоны журнала остаются в модуле
чтения и влияют на разрывы и полноту. Они не превращаются в успешные строки.

## Метрики временной шкалы

`/metrics` публикует монотонные счётчики работы временной шкалы:
`kronika_web_overview_durable_hits_total`,
`kronika_web_overview_fallback_hits_total`,
`kronika_web_overview_rebuilt_total`,
`kronika_web_overview_promotions_total`,
`kronika_web_overview_persistence_failures_total`,
`kronika_web_overview_sealed_failures_total`. Счётчики загрузки фактов
увеличиваются, когда допущенный запрос загружает выбранные факты; первая
публикация содержит только дескрипторы. Продвижение представления показывают
`kronika_web_store_view_generation`,
`kronika_web_overview_view_generation`,
`kronika_web_overview_data_through_us` и
`kronika_web_overview_refresh_errors_total`.

Состояние долговременной записи показывают
`kronika_web_overview_persist_{mode,failures,retry_after_seconds,probe_in_flight}`,
взаимоисключающие индикаторы с закрытым набором значений меток
`kronika_web_overview_persist_reason{reason}` и
`kronika_web_overview_persist_failure_class{class}`, а также
`kronika_web_overview_persist_probe_{attempts,failures,skipped}_total`. GC
публикует индикаторы полноты сканирования, разрешения удаления, превышения
квоты, ожидающих файлов и числа просмотренных записей; индикаторы файлов,
логических и выделенных байтов для закрытых классов
`kind={sidecar,temporary,lock}`; счётчики пропусков, удалённых файлов и
отвязанных логических/выделенных байтов. «Отвязанные выделенные байты» —
значение `st_blocks` открытого файла перед удалением имени; открытый дескриптор
или другая жёсткая ссылка могут сохранить эти блоки на диске.

Загрузка реестра курсоров видна в `kronika_web_timeline_cursor_views`,
`kronika_web_timeline_cursor_bytes` и
`kronika_web_timeline_cursor_pins_total`,
`kronika_web_timeline_cursor_resolves_total`,
`kronika_web_timeline_cursor_evictions_total`,
`kronika_web_timeline_cursor_expired_total` и
`kronika_web_timeline_cursor_capacity_rejections_total`. Работу хранилища
ответов и координации одинаковых задач отражают
`kronika_web_timeline_response_cache_{hits,misses,evictions}_total`,
`kronika_web_timeline_response_cache_{entries,bytes}` и
`kronika_web_timeline_singleflight_{leaders,joins}_total`. Политику выбранных
сегментов показывают `kronika_web_timeline_selected_segments_limit` и
`kronika_web_timeline_query_limit_rejections_total{resource="selected_segments"}`.
Перегрузку холодной загрузки фактов считает
`overview_cold_reject_total{reason}` с закрытым набором причин
`queue_full`, `weight_exceeds_capacity` и `timeout`. Загрузку планировщика
показывают `overview_cold_wait_seconds`, `overview_cold_queue_depth`,
`overview_cold_work_inflight{kind="workers"}`,
`overview_inflight_bytes{kind="pgm"|"decoded"}` и `overview_open_files`.
Совместимый счётчик
`kronika_web_overview_cold_work_rejections_total{reason="capacity"}`
инкрементируется при HTTP-ответах о перегрузке.

Чтение декодированных фактов из памяти и сайдкар OVF показывают
`overview_fact_lookup_total{layer,result,reason}`,
`overview_fact_read_bytes`, `overview_pgm_body_read_bytes`,
`overview_pgm_sections_decoded`,
`overview_fact_build_total{result,source_type}` и
`overview_fact_build_seconds`; записи считает `overview_fact_write_bytes`.
Слой декодированных фактов в памяти публикует
`overview_cache_{entries,bytes}{class="decoded_facts"}` и
`overview_cache_evictions_total{class="decoded_facts",reason}`.

В перечень эксплуатационных метрик `parity-v1` также входят взаимоисключающая
`overview_cache_mode{mode,reason}`, `overview_persist_failures_total{reason}`,
`overview_persist_backoff_seconds`,
`overview_singleflight_{builds,waiters}`,
`overview_live_state{state,reason}`, `overview_live_folded_parts_total`,
`overview_live_data_through_us`, `overview_live_visibility_lag_seconds`,
`overview_view_generation`, `overview_cursor_views`,
`overview_cursor_view_bytes` и `overview_cursor_expired_total{reason}`.
Корректность показывают `overview_source_failures_total{reason}`,
`overview_coverage_loss_total{source,factor,reason}`,
`overview_retained_observations_total{kind}`,
`overview_overflow_total{kind}`, `overview_raw_fallback_total{reason}`,
`overview_gc_files_total{action,reason}` и
`overview_gc_bytes_total{action}`. При сборке маршрутизатора для каждого имени
регистрируются тип и справочный текст. Динамические значения источника и фактора
берутся только из ограниченных опубликованных перечней; остальные значения
меток имеют закрытый набор.

Целостность источника и частичные результаты показывают
`overview_source_read_failures_total{outcome="partial"}`,
`overview_source_scrub_total{outcome}`,
`overview_source_scrub_bytes_total`, `overview_source_scrub_seconds` и
`overview_source_damaged_segments`. Наборы меток этих метрик, планировщика и
хранилищ памяти фиксированы; метки HTTP-запросов используют шаблоны
сопоставленных маршрутов, а не исходный URI.

## Завершение и отказы

`SIGTERM` и `SIGINT` запускают корректное завершение HTTP-сервера. Ошибка
обновления записывается в журнал, а последнее опубликованное представление
остаётся доступным; после заданного порога `/readyz` сообщает устаревшее
состояние. Если каталог источников успешно просмотрен, но построить временную
шкалу не удалось, сервер публикует свежие метаданные вместе с последним
пригодным представлением и не показывает частично собранный результат. Неверная
конфигурация окружения, ошибка первого открытия каталога или фактов либо
недоступная энтропия ОС для аутентификации курсора завершают процесс до
открытия порта.

## BDD перезапуска и восстановления реального процесса

Матрица BDD для PostgreSQL 15–18 запускает
`timeline_web_lifecycle.feature` с собранным исполняемым файлом
`pg_kronika-web`, а не с маршрутизатором внутри процесса и не с заново
созданным `AppState`. Каждый сценарий копирует готовый PGM коллектора в
собственный временный каталог и последовательно запускает несколько новых
процессов с этим каталогом. Настоящие HTTP-запросы проверяют создание
сайдкар-файла OVF, неизменность inode, времени изменения и хеша при чтении
после перезапуска, отсутствие чтений тел PGM и декодирования секций,
восстановление повреждённых и устаревших заголовков, обработку остатка
прерванной публикации, ограниченный резервный кэш при ошибке записи, истечение
курсора прошлого процесса и предсказуемую конкуренцию владельцев записи.

Готовность подтверждается сообщением после открытия порта, а штатное
завершение процесса проверяется по коду выхода. Для остановки публикации и
конкуренции используется доступный только в квалификационной сборке
Unix-сокет: барьер срабатывает после синхронизации временного OVF, но до
атомарного переименования. Результат не зависит от задержек и циклов повторных
попыток. Запуск всей матрицы жизненного цикла:

```sh
DEBUG=1 make test-bdd TAGS=@timeline_web_lifecycle
```

Запуск для одной поддерживаемой версии, например PostgreSQL 15:

```sh
DEBUG=1 make test-bdd TAGS='@timeline_web_lifecycle and @pg15'
```

У бинарника нет параметров командной строки. MCP, удалённые хранилища,
управление сроком хранения исходных сегментов и доставка оповещений не
реализованы.
