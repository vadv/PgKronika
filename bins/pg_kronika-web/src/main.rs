//! Serves data to humans and agents: web UI, MCP server, JSON API.
//!
//! Not implemented yet: the workspace is in the skeleton stage,
//! see `docs/plan.md`.

fn main() {
    eprintln!("pg_kronika-web: not implemented yet, see docs/plan.md");
    std::process::exit(2);
}
