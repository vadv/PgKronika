# Only stable options: the toolchain is pinned to stable (rust-toolchain.toml),
# unstable options would be silently ignored there.
style_edition = "2024"
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
